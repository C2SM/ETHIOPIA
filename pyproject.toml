[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
# See https://www.python.org/dev/peps/pep-0621/
name = 'sirocco'
dynamic = ["version"]
description = 'A library for creating weather and climate workflows.'
authors = [
  {name = 'Matthieu Leclair', email = 'matthieu.leclair@env.ethz.ch'},
  {name = 'Julian Geiger', email = 'julian.geiger@psi.ch'},
  {name = 'Alexander Goscinski', email = 'alexander.goscinski@psi.ch'},
]
readme = 'README.md'
classifiers = [
  'Development Status :: 1 - Planning',
  'Intended Audience :: Science/Research',
  'Operating System :: POSIX :: Linux',
  'Programming Language :: Python',
  'Programming Language :: Python :: 3.10',
  'Programming Language :: Python :: 3.11',
  'Programming Language :: Python :: 3.12',
  'Programming Language :: Python :: 3.13',
  'Topic :: Scientific/Engineering',
  'Topic :: Scientific/Engineering :: Atmospheric Science',
  'License :: OSI Approved :: MIT License',
]
keywords = ["workflow"," icon", "aiida", "aiida-workgraph"]
requires-python = '>=3.10'
dependencies = [
  "isoduration",
  "pydantic",
  "ruamel.yaml",
  "aiida-core==2.7.3",
  # pinned to my branch with the temporary fix
  "aiida-firecrest @ git+https://github.com/aiidateam/aiida-firecrest.git@c4e287ffe40ae57db7032fe1c80d2c4def0dc7fa",
  # pinned on my branch that contains all the changes for DYAMOND, so far
  "aiida-icon @ git+https://github.com/aiida-icon/aiida-icon.git@6a15937b3656242cfd9812b1dd5046216547b9ca",  
  # pinned on main
  "aiida-workgraph @ git+https://github.com/aiidateam/aiida-workgraph.git",
  # aiida-shell
  "aiida-shell>=0.8.2",
  "aiida-gui",
  "ipdb",
  "termcolor",
  "pygraphviz==2.0",
  "lxml",
  "f90nml",
  "aiida-shell>=0.8.1",
  "rich",
  "typer",
  "aiida-gui-workgraph>=0.1.3",
  "jinja2>=3.0",
  "types-PyYAML",
  "coverage",
  "isodate",
  "dotenv"
]
license = {file = "LICENSE"}

[project.urls]
Repository = "https://github.com/C2SM/Sirocco.git"
Documentation = "https://c2sm.github.io/Sirocco/"
Changelog = "https://github.com/C2SM/Sirocco/blob/main/CHANGELOG.md"

[project.scripts]
sirocco = "sirocco.cli:app"

[tool.pytest.ini_options]
# Configuration for [pytest](https://docs.pytest.org)
log_level = "ERROR"
log_cli = true
log_cli_level = "ERROR"
log_cli_format = "%(asctime)s [%(levelname)s] %(name)s: %(message)s"
addopts = "--pdbcls=IPython.terminal.debugger:TerminalPdb"
norecursedirs = "tests/cases"
markers = [
  "slow: slow integration tests which are not recommended to run locally for normal development",
  "requires_icon: marks test to require icon installation"
]

filterwarnings = [
  "ignore::UserWarning",
  'ignore:datetime.datetime.utcfromtimestamp\(\) is deprecated:DeprecationWarning',  # from aio_pika via duration
  "ignore:There is no current event loop:DeprecationWarning",  # from plumpy via aiida testing tools
  "ignore:Object of type <DbNode> not in session:sqlalchemy.exc.SAWarning",  # sqlalchemy via aiida testing tools
]

[tool.coverage.run]
# Configuration of [coverage.py](https://coverage.readthedocs.io)
# reporting which lines of your plugin are covered by tests
source = ["sirocco"]

[tool.ruff]
include = ["src/*py", "tests/*py"]
target-version = "py312"

[tool.ruff.lint]
ignore = [
  "TID252", # prefer relative import over absolute
  "TRY003", # write custom error messages for formatting
  "PLC0415", # import should be at the top-level of a file (lazy imports)
  "N806", # variable in function should be lowercase
  "SLF001", # private member accessed

]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = [
  "T201",  # No use of `print`
  "S101",  # No use of `assert`
  "PLR2004",  # No use of magic values
  "S607",  # Starting a process with a partial executable path
  "RUF015",  # Prefer `next` over single element slice
]

## Hatch configurations

[tool.hatch.build.targets.sdist]
include = [
  "src/sirocco/",
]

[tool.hatch.version]
path = "src/sirocco/__init__.py"

[tool.hatch.metadata]
allow-direct-references = true  # editable installs

[tool.hatch.envs.default]
installer = "uv"
python = "3.12"

[tool.hatch.envs.hatch-test]
installer = "uv"
extra-dependencies = [
    "ipdb",
    "pytest-cov",
    "pytest-sugar",  # Nicer pytest output
    "pytest-xdist",  # Parallel test execution
    "coverage",      # For showing coverage gutter in IDEs
]
default-args = []
extra-args = ["--doctest-modules", "-m", "not slow and not requires_icon"]

[[tool.hatch.envs.hatch-test.matrix]]
python = ["3.12"]

[tool.hatch.envs.docs]
installer = "uv"
description = "Build the documentation"
dependencies = [
    "mkdocs-material",
]

[tool.hatch.envs.docs.scripts]
build = [
  "mkdocs build -f docs/mkdocs.yml",
  "echo 'Now open docs/build/index.html with your browser'"
]
serve = [
  "mkdocs serve -f docs/mkdocs.yml",
]

deploy = [
  "mkdocs gh-deploy --no-history -f docs/mkdocs.yml"
]

[tool.hatch.envs.types]
python = "3.12"
extra-dependencies = [
  "mypy>=1.0.0",
  "pytest",
  "lxml-stubs",
  "types-setuptools",
  "types-docutils",
  "types-colorama",
  "types-Pygments",
  "types-termcolor",
  "types-requests",
  "types-PyYAML",
  "ty",
]

[tool.hatch.envs.types.scripts]
check = "mypy --exclude 'tests/cases/*' --no-incremental {args:.}"
ty-check = "ty check --exclude 'tests/cases/*' --python-version 3.12"

[tool.mypy]
disable_error_code = ["import-untyped"]
exclude = ["^tests/"]

[[tool.mypy.overrides]]
module = ["isoduration", "isoduration.*"]
follow_untyped_imports = true

[[tool.mypy.overrides]]
module = ["pygraphviz"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["f90nml"]
ignore_missing_imports = true
